import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    valueContainer: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    value: {
        fontSize: 26,
        fontWeight: 'bold',
    },
    label: {
        marginTop: 4,
        fontSize: 16,
        color: '#666',
    },
});